import React from 'react';
import PropTypes from 'prop-types';

const Consecutive = (props) => {
  return(
  	<div>Consecutive</div>
  );
};

Consecutive.propTypes = {
  props: PropTypes.object.isRequired
};

export default Consecutive;