import React from 'react';
import PropTypes from 'prop-types';

const Iva = (props) => {
  return(
  	<div>Iva</div>
  );
};

Iva.propTypes = {
  props: PropTypes.object.isRequired
};

export default Iva;