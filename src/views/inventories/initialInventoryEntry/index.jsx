import React from 'react';
import PropTypes from 'prop-types';

const InitialInventoryEntry = (props) => {
  return(
  	<div>InitialInventoryEntry</div>
  );
};

InitialInventoryEntry.propTypes = {
  props: PropTypes.object.isRequired
};

export default InitialInventoryEntry;