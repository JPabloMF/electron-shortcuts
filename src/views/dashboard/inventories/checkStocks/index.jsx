import React from 'react';
import PropTypes from 'prop-types';

const CheckStocks = (props) => {
  return(
  	<div>CheckStocks</div>
  );
};

CheckStocks.propTypes = {
  props: PropTypes.object.isRequired
};

export default CheckStocks;