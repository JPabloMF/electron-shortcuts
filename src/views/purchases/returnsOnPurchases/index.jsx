import React from 'react';
import PropTypes from 'prop-types';

const ReturnsOnPurchases = (props) => {
  return(
  	<div>ReturnsOnPurchases</div>
  );
};

ReturnsOnPurchases.propTypes = {
  props: PropTypes.object.isRequired
};

export default ReturnsOnPurchases;