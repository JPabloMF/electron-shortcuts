import React from 'react';
import PropTypes from 'prop-types';

const MakePurchases = (props) => {
  return(
  	<div>MakePurchases</div>
  );
};

MakePurchases.propTypes = {
  props: PropTypes.object.isRequired
};

export default MakePurchases;