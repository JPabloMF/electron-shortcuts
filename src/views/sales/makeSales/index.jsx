import React from 'react';
import PropTypes from 'prop-types';

const MakeSales = (props) => {
  return(
  	<div>MakeSales</div>
  );
};

MakeSales.propTypes = {
  props: PropTypes.object.isRequired
};

export default MakeSales;