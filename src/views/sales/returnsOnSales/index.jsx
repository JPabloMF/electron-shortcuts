import React from 'react';
import PropTypes from 'prop-types';

const ReturnsOnSales = (props) => {
  return(
  	<div>ReturnsOnSales</div>
  );
};

ReturnsOnSales.propTypes = {
  props: PropTypes.object.isRequired
};

export default ReturnsOnSales;